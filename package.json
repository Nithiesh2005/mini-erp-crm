{
  "name": "mini-erp-crm",
  "version": "1.0.0",
  "private": true,
  "description": "Mini ERP + CRM Operations Portal (Express + Prisma + React)",
  "scripts": {
    "install:all": "npm install && npm --prefix server install && npm --prefix client install",
    "db:setup": "npm --prefix server run migrate && npm --prefix server run seed",
    "dev": "concurrently -n server,client -c blue,green \"npm --prefix server run dev\" \"npm --prefix client run dev\"",
    "smoke": "npm --prefix server run smoke"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
